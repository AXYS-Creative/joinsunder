---
layout: app
path: sunder-way

title: The Sunder Way
description: #CHANGE ME

# master props
hide_header: false
hide_footer: false
---

{% if page_sunder_way.ethos.toggle_section %}
<section class="section-ethos">
  <div class="ethos-pin">
    <div class="ethos-top">
      <h1 class="ethos-top__title">
        {{ page_sunder_way.ethos.section_title }}
      </h1>
      <figure class="ethos-top__figure">
        {% for img in page_sunder_way.ethos.section_imgs %}
        <img
          class="ethos-top__figure-img ethos-img-{{ loop.index }}"
          src="{{ img.src }}"
          alt=""
        />
        {% endfor %}
      </figure>
    </div>
    <div class="ethos-middle">
      <p class="ethos-middle__p">{{ page_sunder_way.ethos.section_desc }}</p>

      <div class="ethos-middle__panels">
        <!-- prettier-ignore -->
        {% for item in page_sunder_way.ethos.values %}
        <div
          class="ethos-middle__panels-panel value-panel value-panel-{{
            loop.index
          }}"
        >
          <h2 class="panel-title">{{ item.title }}</h2>
          <p class="panel-desc">{{ item.caption }}</p>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>
  <div class="ethos-bottom">
    <p class="ethos-bottom__p">{{ page_sunder_way.ethos.cta_p }}</p>

    <!-- prettier-ignore -->
    {% set cta_1_text = page_sunder_way.ethos.cta.text %}
    {% set cta_1_url = page_sunder_way.ethos.cta.url %}
    {% set cta_1_outline = true %}
    {% include "./_includes/components/cta-group.njk" %}
  </div>
  <div class="ethos-pin-steps">
    <div class="ethos-pin-step ethos-pin-step-1"></div>
    {% for item in page_sunder_way.ethos.values %}
    <div class="ethos-pin-step ethos-pin-step-{{ loop.index + 1 }}"></div>
    {% endfor %}
  </div>
</section>
{% endif %}

<!-- prettier-ignore -->
{% if page_sunder_way.competitions.toggle_section %}
<section class="section-competitions" id="competitions">
  <h2 class="section-competitions__title">
    {{ page_sunder_way.competitions.section_title }}
  </h2>
  <p class="section-competitions__desc">
    {{ page_sunder_way.competitions.section_desc }}
  </p>

  <div class="competition-types">
    {% for type in page_sunder_way.competitions.types %}
    <figure class="competition-types__figure">
      <img class="competition-types__img" src="{{ type.src }}" alt="" />

      <figcaption class="competition-types__caption">
        <h3 class="competition-types__caption-title">{{ type.title }}</h3>
        <p class="competition-types__caption-p">{{ type.caption }}</p>
      </figcaption>
    </figure>
    {% endfor %}
  </div>

  <div class="yog">
    <figure class="yog-logo">
      <img
        class="yog-logo__img"
        src="{{ page_sunder_way.competitions.yog_logo }}"
        alt="Year of Gear"
        eleventy:ignore
      />
    </figure>

    <div class="yog-merch">
      {% for row in page_sunder_way.competitions.yog_merch %}
      <div class="yog-merch__row">
        {% for item in row.items %}
        <img
          class="yog-merch__row-img gsap-animate {{ item.rotate }}"
          src="{{ item.src }}"
          alt="{{ item.title }}"
          loading="eager"
        />
        {% endfor %}
      </div>
      {% endfor %}
    </div>

    <div class="yog-types">
      {% for type in page_sunder_way.competitions.yog_types %}
      <img class="yog-types__img" src="{{ type.src }}" alt="" eleventy:ignore />
      {% endfor %}
    </div>

    <div class="yog-stripes">
      <div class="yog-stripes__stripe"></div>
    </div>
  </div>
</section>
{% endif %}

<!-- <iframe
  class="sample-video-embed"
  src="https://drive.google.com/file/d/19so7OJN9FwQUdJ7sfoY-n-lZoCV8iWz9/preview"
  width="640"
  height="480"
  allow="autoplay"
  allowfullscreen
></iframe> -->

<!-- prettier-ignore -->
{% set section_id = "operation-sunder-serves" %}
{% set section_graphic = page_sunder_way.oss.graphic %}
{% set graphic_title = page_sunder_way.oss.section_title | removeTokens %}
{% set section_h1 = '' %}
{% set section_h2 = page_sunder_way.oss.section_title %}
{% set section_p = page_sunder_way.oss.section_desc %}
{% set section_bg = page_sunder_way.oss.section_bg %}
{% set cta_1_text = page_sunder_way.oss.cta.text %}
{% set cta_1_url = page_sunder_way.oss.cta.url %}
{% set cta_1_outline = false %}
{% set is_hero = false %}
{% set container_tag = 'section' %}
{% set section_img_hero_class = 'oss' %}
{% if page_sunder_way.oss.toggle_section %}
  {% include "./_includes/partials/partial-img-hero.njk" %}
{% endif %}

<!-- prettier-ignore -->
{% if page_sunder_way.quotes.toggle_section %}
  {% set section_id = 'quote-section' %}
  {% set section_title = page_sunder_way.quotes.section_title %}
  {% set quote_list = page_sunder_way.quotes.list %}
  {% include "./_includes/partials/partial-quote.njk" %}
{% endif %}
